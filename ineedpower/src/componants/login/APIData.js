//fetch code: https://facebook.github.io/react-native/docs/network
/*
export function APIData(userData) {
  //userdata is obtained from canvas

  let BaseUrl = "https://ehb.instructure.com/api/v1/users/self";

  return new Promise((resolve, reject) => {
    fetch(BaseUrl, {
      method: "POST",
      body: JSON.stringify(userData)
    })
      .then(response => response.json())
      .then(responseJSON => {
        resolve(responseJSON);
      })
      .catch(error => {
        reject(error);
      });
  });
}
*/
